# Workspace Rules for Tourist Route Planner

- Always commit and push changes for each function or distinct component separately with a clear conventional commit message (e.g., `refactor: optimize UCS stale-entry handling` or `test: add A* regression coverage`).
